#include <iostream>
struct Time{
    int hr;
    int min;
};

int main() {
	int n,min_count,count=0;
	bool tuition=false;
	std::cin>>n>>min_count;
	struct Time actual;
	std::cin>>actual.hr;
	std::cin.ignore(1,':');
	std::cin>>actual.min;
	struct Time times[n];
	for(int i=0;i<n;i++){
	    std::cin>>times[i].hr;
	    std::cin.ignore(1,':');
	    std::cin>>times[i].min;
	    if(times[i].hr>actual.hr){
	        count++;
	    }
	    else if(times[i].hr==actual.hr){
	        if(times[i].min>actual.min){
	            count++;
	        }
	    }
	    if(count>n-min_count){
	        tuition=true;
	    }
	}
	tuition?std::cout<<"Yes":std::cout<<"No";
	return 0;
}
